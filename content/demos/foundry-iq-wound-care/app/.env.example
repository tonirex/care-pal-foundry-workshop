# Foundry IQ wound-care demo — engineer client config
# Copy to .env and fill in your values.

# Your Foundry project endpoint (Foundry portal → project Home / Settings)
# e.g. https://<account>.services.ai.azure.com/api/projects/<project>
PROJECT_ENDPOINT=your_project_endpoint

# The agent you created in ../README.md
AGENT_NAME=woundcare-iq-agent
